#![warn(clippy::all, clippy::nursery, clippy::pedantic, clippy::cargo)]

#[allow(unused_imports, reason = "TBD")]
use maex::load_index;

fn main() {
    // let left = 2;
    // let right = 2;
    // let result = add(left, right);
    // println!("The sum of {left} and {right} is {result}");
}
